<section class="tasks-section hidden-mobile">
  <% if (tasksData) { %>

    <ul class="tasks-list">
      <h1>Tasks</h1>
      <% tasksData.map(function(elem) { %>
        <li class="task" onclick="setSelected('<%= elem.id %>')">
          <%= elem.name %>
        </li>
      <% }) %>
    </ul>

    <% tasksData.map(function(task) { %>
      <ul class="todos-list hidden-todos id-<%= task.id %>">
        <h1><%= task.name %></h1>
        <% task.todos.map(function(todo) { %>
        <li>
          <%= todo.text %>
        </li>
        <% }) %>
      </ul>
    <% }) %>


  <% } else { %>

    <p>No task data!!!</p>

  <% } %>
</section>

<script type="text/javascript" src="js/tasks.js"></script>